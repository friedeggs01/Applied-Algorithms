#include<stdio.h>
void TRY(int k){
    for(int v=1; v<=n; v++){
        if(marked[v]== false)
    }
}
void process() {
    if(f + c[x[n]][x[1]] < f_min){
        f_min = f + c[x[n]][x[1]];
    }
}

int main() {
    f_min = 1e9;
    for(int v=1; v<=n; v++){
        marked[v] = false;
    }
    x[1]=1;
    marked[1] = true;
    TRY(2);
}