#include<bits/stdc++.h>
using namespace std;
struct node{
    int data;
    node *dad;
};

int main() {
    freopen("6.txt", "r", stdin);
    ios_base::sync_with_stdio(false);
    cin.tie();
    node* root =;
    while(true) {
        string temp;
        cin >> temp;
        if(temp == "#") {
            break;
        } else if (temp == "descendants") {
            string
        }
    }
}