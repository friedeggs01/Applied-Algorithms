#include<bits/stdc++.h>
using namespace std;



int main() {
    ios_base::sync_with_stdio();
    cin.tie(); cout.tie();
    int t, n, a[101];
    while(t--){
        cin >> n;
        for (int i = 0; i < n; ++i) {
            cin >> a[i];
            if(a[i] %= 2)
        }


    }
}